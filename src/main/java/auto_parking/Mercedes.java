package auto_parking;

/**
 * Created BY Armine Movsisyan on 04 February,2020
 */
 class Mercedes extends  Car {

    public Mercedes(int speed, String model, boolean stateEngine, int maxSpeed, CarType type) {
        super(speed, model, stateEngine, maxSpeed, type);
    }
}
